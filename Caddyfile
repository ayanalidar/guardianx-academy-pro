# GuardianX Academy — Caddy reverse proxy
#
# SECURITY: The previous `XTransformPort` handler allowed any client to make
# Caddy proxy to an arbitrary localhost port (SSRF). It has been removed.
# If you need per-service routing, add explicit `:PORT` blocks below or use
# Caddy's named routes via the admin API.

:81 {
	# Web app (default)
	handle {
		reverse_proxy localhost:3000 {
			header_up Host {host}
			header_up X-Forwarded-For {remote_host}
			header_up X-Forwarded-Proto {scheme}
			header_up X-Real-IP {remote_host}
		}
	}
}

# === Explicit per-service routes (add as needed) ===
# Example: expose the WebRTC signaling server on its own path.
# :81/live {
#   reverse_proxy localhost:3003
# }
#
# Example: lab orchestrator (should be internal-only — do NOT expose publicly).
# :81/lab {
#   @internal client_ip 10.0.0.0/8 192.168.0.0/16
#   handle @internal {
#     reverse_proxy localhost:3004
#   }
#   respond 403
# }
